import Logout from "@/components/auth/logout";
import React from "react";

const page = () => {
  return (
    <div>
      <h1>Dashboard</h1>
      <Logout />
    </div>
  );
};

export default page;
